// SimpleAlarm ArduinoML file converted

application SimpleAlarm {

  actuator led    : 12;
  actuator buzzer : 11;
  sensor button   : 8;

  -> off {
    led <= LOW;
    buzzer <= LOW;
    button is HIGH => on;
  }

  on {
    led <= HIGH;
    buzzer <= HIGH;
    button is LOW => off;
  }
}
